pkgname=meta-profile
pkgver=1.0
pkgrel=1
arch=('any')
source=("profile")
license=('MIT')
depends=('fcitx' 'fcitx-configtool')
md5sums=('SKIP')

package() {
	mv "${pkgdir}/etc/profile" "${pkgdir}/etc/profile.backup"
    	install -Dm755 "${srcdir}/profile" "${pkgdir}/etc/profile"
}
